{
	"name": "@typeskrift/fast-memoize-decorator",
	"version": "0.1.0",
	"description": "A TypeScript decorator for memoizing properties using fast-memoize.",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git@github.com:typeskrift/fast-memoize-decorator.git"
	},
	"author": "Brian Faust <brian@konceiver.dev>",
	"homepage": "https://github.com/typeskrift/fast-memoize-decorator",
	"bugs": {
		"url": "https://github.com/typeskrift/fast-memoize-decorator/issues"
	},
	"main": "dist/index",
	"scripts": {
		"build": "tease build",
		"format": "tease format",
		"lint": "tease lint",
		"prepare": "tease install && tease build",
		"publish": "npm run prepare && npm publish --access=public",
		"specs": "tease specs",
		"test": "tease test"
	},
	"files": [
		"dist"
	],
	"dependencies": {
		"fast-memoize": "^2.5.2"
	},
	"devDependencies": {
		"@konceiver/tease": "^2.0.0"
	},
	"types": "dist/index"
}
